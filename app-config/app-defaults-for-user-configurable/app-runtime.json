{
  "_comment": "Tracked, pure-data app configuration. Settings exposed in the dashboard UI (OOR threshold, slippage, intervals, etc.) live in app-config/app-defaults-for-user-configurable/bot-config-defaults.json instead, with per-install operator overrides in app-config/user-configurable/bot-config-defaults.json and per-position runtime overrides in app-config/user-configurable/bot-config.json.",
  "server": {
    "port": 5555,
    "host": "127.0.0.1"
  },
  "defaults": {
    "_comment": "chain: which entry of chains.json the app runs against when CHAIN_NAME is not set in .env. Read by src/runtime-flags.js, which lowercases it and looks it up in chains.json; a value matching no entry there throws at startup rather than falling back to another chain, since the entry carries the RPC endpoints and contract addresses every transaction is built against. Per feedback_one_literal_per_shipped_default the chain name lives here only.",
    "chain": "pulsechain"
  },
  "tx": {
    "_comment": "TX recovery pipeline timing, cross-chain. speedupSec: after this many seconds of the original TX not confirming, submit a same-nonce replacement with 1.5x gas (Phase 2 of _waitOrSpeedUp). deadlineSec: on-chain contract deadline (`require(block.timestamp <= deadline)`) stamped into removeLiquidity / swap / mint calldata. cancelToDeadlineMultiple: the bot's per-TX give-up timer (TX_CANCEL_SEC in code) is derived as deadlineSec × cancelToDeadlineMultiple — how much total time to wait before sending a 0-PLS self-transfer cancel at the stuck nonce. Rule of thumb: set the multiple ≥ 4 so a slow-chain retry cycle can complete before the bot gives up on the nonce. See src/config.js for the derivation and src/rebalancer-pools.js for the deadline enforcement.",
    "speedupSec": 120,
    "deadlineSec": 900,
    "cancelToDeadlineMultiple": 4,
    "retrySwapLimit": 8
  },
  "rescanPrices": {
    "_comment": "How long the dashboard's Re-scan Prices dialog waits for the scan it started before handing the button back, rather than spinning forever. Read by public/dashboard-rescan-prices.js via the rescanPricesTimeoutMs field on GET /api/status. It bounds a button, not a scan: nothing server-side reads it, and the scan it is waiting on runs to completion either way.",
    "timeoutMs": 7200000
  },
  "compound": {
    "minFeeUsd": 1,
    "defaultThresholdUsd": 5
  },
  "log": {
    "file": "./app-data/rebalance_log.json"
  },
  "aggregator": {
    "_comment": "AGGREGATOR_API_KEY is a public 0x-api-key embedded in 9mm's product (required for valid Aggregator calldata) — not a user-owned credential. It lives here, not in api-keys.json, because encrypting it would force every operator to re-enter it, which is not their job. Override per-deployment via AGGREGATOR_URL / AGGREGATOR_API_KEY env vars if needed.",
    "url": "https://api.9mm.pro",
    "apiKey": "f9275849-2a1d-406b-b2a2-a6be1ac127dc"
  }
}
