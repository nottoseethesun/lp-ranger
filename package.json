{
  "name": "9mm-v3-position-manager",
  "version": "2.0.0",
  "description": "Auto-rebalancing position manager for 9mm v3 (Uniswap v3 fork) on PulseChain",
  "main": "server.js",
  "scripts": {
    "build": "esbuild public/dashboard-init.js --bundle --outfile=public/dist/bundle.js --format=iife --platform=browser --target=es2020 && node -e \"const fs=require('fs'),p='public/index.html',h=fs.readFileSync(p,'utf8');fs.writeFileSync(p,h.replace(/bundle\\.js\\?v=[^\\\"']*/,'bundle.js?v='+Date.now()))\" && echo 'Cache-bust: bundle.js?v='$(date +%s%3N)",
    "build:watch": "esbuild public/dashboard-init.js --bundle --outfile=public/dist/bundle.js --format=iife --platform=browser --target=es2020 --watch",
    "copy-fonts": "bash scripts/copy-fonts.sh",
    "postinstall": "bash scripts/copy-fonts.sh",
    "start": "node server.js",
    "build-and-start": "npm run build && node server.js",
    "dev": "npm run build && node --watch server.js",
    "bot": "node bot.js",
    "stop": "bash scripts/stop.sh",
    "reset-wallet": "rm -f .wallet.json && sed -i 's/^WALLET_PASSWORD=.*/WALLET_PASSWORD=/' .env 2>/dev/null; echo 'Wallet cleared — re-import via dashboard on next start'",
    "clean": "npm run stop 2>/dev/null; npm run reset-wallet && rm -f .bot-config.json .epoch-cache.json rebalance_log.json tmp/event-cache.json && echo 'Cleaned: server stopped, wallet, bot config, epoch cache, rebalance log, event cache' && echo 'TIP: Also clear browser localStorage & cookies via the Settings gear icon in the dashboard.'",
    "check": "bash scripts/check.sh",
    "lint": "eslint src/ test/ server.js bot.js public/dashboard-*.js eslint-rules/ --max-warnings 0 && stylelint public/*.css",
    "lint:fix": "eslint src/ test/ server.js bot.js public/dashboard-*.js eslint-rules/ --fix && stylelint public/*.css --fix",
    "test": "node --test test/*.test.js test/eslint-rules/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage test/*.test.js test/eslint-rules/*.test.js",
    "test:watch": "node --test --watch test/*.test.js test/eslint-rules/*.test.js",
    "knip": "knip"
  },
  "dependencies": {
    "@uniswap/v3-periphery": "^1.4.4",
    "dotenv": "^16.0.0",
    "ethers": "^6.7.1",
    "navigo": "^8.11.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@fontsource/space-mono": "^5.2.9",
    "@fontsource/urbanist": "^5.2.7",
    "esbuild": "^0.27.4",
    "eslint": "^10.0.3",
    "globals": "^17.4.0",
    "knip": "^5.86.0",
    "stylelint": "^17.4.0",
    "stylelint-config-standard": "^40.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "Apache-2.0"
}
